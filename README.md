Вот обновлённый `README.md` с учётом последних исправлений в коде, улучшенной структурой и пояснениями по запуску бота и графика.  

---

# **NoSos**
NoSos — это инструмент мониторинга активности игроков на сервере Minecraft. Он отображает позиции игроков, создаёт тепловые карты, следит за нарушениями и отправляет уведомления через Telegram-бота.

---

## **🚀 Возможности**
### 🔍 **Мониторинг игроков в реальном времени**
- Отображение позиций игроков на карте.
- Список онлайн-игроков с координатами.
- Отслеживание активности по времени суток.

### 🎨 **Генерация тепловых карт**
- Визуализация активности игроков на карте.
- Сохранение истории активности.
- Настройка цветовой палитры и прозрачности.

### 🚨 **Оповещения**
- Вход в запрещённые зоны.
- Превышение лимита игроков.
- Подозрительные перемещения (читерство).
- Уведомления через Telegram-бота.

### 🔧 **Гибкая настройка**
- Задание зон с разными уровнями оповещений (`INFO`, `WARNING`, `CRITICAL`).
- Исключённые зоны (без уведомлений).
- Разрешённые игроки для каждой зоны.

### 📊 **Экспорт данных**
- Сохранение статистики игроков и зон в CSV, JSON и Excel.
- Экспорт времени, проведённого игроками в зонах.

### ⚡ **Производительность**
- Кэширование данных.
- Многопоточная обработка событий.
- Оптимизированная работа с базой данных.

---

## **📥 Установка и запуск**

### **Требования**
- **Python 3.8+**
- Установленные зависимости (см. ниже).

### **🔧 Установка**
1. **Клонируйте репозиторий:**
   ```sh
   git clone https://github.com/your-repo/NoSos.git
   cd NoSos
   ```
2. **Установите зависимости:**
   ```sh
   pip install -r requirements.txt
   ```
3. **Настройте конфигурацию:**
   - Отредактируйте `config/config.yaml`.
   - Укажите **токен бота** и **ID чата** в `config/.env`:
     ```
     TELEGRAM_TOKEN=ВАШ_ТОКЕН
     TELEGRAM_CHAT_ID=ВАШ_CHAT_ID
     ```

### **▶ Запуск**
```sh
python run.py
```
После запуска:
- Откроется график с картой и тепловыми точками игроков.
- В консоли будет выводиться информация о работе.
- Telegram-бот начнёт принимать команды.

---

## **⚙ Конфигурация**
Все настройки хранятся в `config.yaml`.

### **📌 Основные параметры**
```yaml
players_url: "https://map.serverchichi.online/maps/world/live/players.json"
update_interval: 15
min_request_interval: 5
```
- **`players_url`** — URL API с данными игроков.
- **`update_interval`** — интервал обновления данных (сек).
- **`min_request_interval`** — минимальный интервал запросов.

### **🌍 Настройки карты**
```yaml
display:
  point_size: 25
  point_color: "#00FF00"
  labels:
    font_size: 8
    text_color: "#FFFF00"
```
- **`point_size`** — размер точки игрока.
- **`point_color`** — цвет точки.
- **`labels`** — подписи игроков.

### **🔥 Настройки тепловой карты**
```yaml
heatmap:
  bins: 100
  cmap: "hot"
  alpha: 0.7
  max_history: 10000000
  history_file: "heatmap_history.pkl"
```
- **`bins`** — детализация карты.
- **`cmap`** — цветовая палитра.
- **`alpha`** — прозрачность.

### **🚧 Настройки зон и алертов**
```yaml
alerts:
  zones:
    - name: "Спавн"
      bounds: {xmin: -500, xmax: 500, zmin: -500, zmax: 500}
      alert_level: "WARNING"
      cooldown: 30
      excluded: True
```
- **`zones`** — список зон.
- **`alert_level`** — уровень тревоги.
- **`excluded`** — зона исключена из мониторинга.

---

## **📡 Telegram-бот**
Бот управляет пользователями и отправляет уведомления о нарушениях.

### **📝 Доступные команды**
| Команда           | Описание |
|------------------|------------------------|
| `/start`         | Регистрация в системе |
| `/help`          | Помощь по командам |
| `/subscribe`     | Подписка на уведомления |
| `/unsubscribe`   | Отключение уведомлений |
| `/history`       | Топ активных игроков |
| `/users` (админ) | Список пользователей |
| `/approve <id>` (админ) | Одобрить пользователя |
| `/send <id> <сообщение>` (админ) | Отправить сообщение |

### **🚀 Добавление бота**
1. Создайте бота в [BotFather](https://t.me/BotFather).
2. Сохраните токен в `config/.env`:
   ```
   TELEGRAM_TOKEN=ваш_токен
   TELEGRAM_CHAT_ID=ваш_chat_id
   ```
3. Запустите бота:  
   ```sh
   python run.py
   ```

---

## **📊 Экспорт данных**
| Формат  | Описание |
|---------|--------------------------------|
| `CSV`   | Экспорт в таблицу |
| `JSON`  | Экспорт в JSON-файл |
| `Excel` | Экспорт в Excel |

📌 Пример команды:
```sh
python run.py --export player_zone_time
```

---

## **🔍 Пример логов**
Логи хранятся в `logs/monitor.log`:
```
2025-01-26 14:30:00 - INFO - Загружено записей истории: 24955
2025-01-26 14:30:01 - WARNING - Игрок Player1 в зоне "Сосмарк"
```

---

## **🛠 Основные классы**
| Класс | Описание |
|------------------|-------------------------------------------|
| `Monitor` | Основной цикл мониторинга |
| `Heatmap` | Генерация тепловой карты |
| `AlertManager` | Управление алертами |
| `SecurityManager` | Проверка прав пользователей |
| `TelegramBot` | Бот для уведомлений |

---

## **📝 Лицензия**
Проект распространяется под лицензией **MIT**. Подробнее в файле [`LICENSE`](LICENSE).

---

## **❓ Поддержка**
Если у вас возникли вопросы, создайте `issue` в репозитории! Мы поможем 🚀

---

### **💡 Краткий FAQ**
> **Бот не отвечает?**  
✅ Проверьте `config/.env` и запустите команду `/start`.

> **Нет графика Matplotlib?**  
✅ Убедитесь, что установлен `matplotlib`:  
```sh
pip install matplotlib
```

> **Как быстро протестировать?**  
✅ Уменьшите `update_interval` в `config.yaml` до `5`.

---

## **🎯 Итог**
✅ **Бот работает**  
✅ **Мониторинг запускается**  
✅ **График отображает игроков**  
✅ **Оповещения приходят в Telegram**  

🚀 **Готово! Установите NoSos и начните мониторинг!** 🚀
